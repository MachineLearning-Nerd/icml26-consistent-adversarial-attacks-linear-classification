# Evaluator entry: Claim 2

The cumulative fixed command is:

```bash
uv sync --frozen && .venv/bin/python repro/src/verify.py
```

The verifier prints the analytic certificate, all per-seed counts, standard
errors, finite-dimensional assumption audits, and controls. It exits nonzero
unless the printed formula is rejected and the definition-faithful formula is
independently recovered.

Status before remote execution: **BLOCKED**.
